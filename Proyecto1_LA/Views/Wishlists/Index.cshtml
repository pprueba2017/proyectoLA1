@model IEnumerable<Proyecto1_LA.Models.Wishlist>

@{
    ViewBag.Title = "Index";
}

<h2>Lista de Sugerencias</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nombre_Usuario)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre_Usuario)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idWishlist }) |
            @Html.ActionLink("Details", "Details", new { id=item.idWishlist }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idWishlist })
        </td>
    </tr>
}

</table>
